"""
data types --> string, int, float, boolean
Type Casting is nothing but a converting one data-type into another data-type
like string -> int

without converting the string into int (or) int into string, if you try to add those
data, you're getting the typeError.

"""

name = "Siva"
age = 20

# if I'm trying to add these two data, I am getting typerError print(name + age).
print(name + " " + str(age))
